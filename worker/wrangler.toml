name = "knockknock-ai-proxy"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# Run `npx wrangler kv namespace create RL` and paste the returned id below.
kv_namespaces = [
  { binding = "RL", id = "1c737c25353d4bf0a7e592b557248863" }
]

# The Anthropic key is NOT stored here. Set it as a secret:
#   npx wrangler secret put ANTHROPIC_API_KEY
